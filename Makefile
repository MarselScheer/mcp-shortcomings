SHELL:=/bin/bash
.ONESHELL:

# ==============================================================================
# Development
# ==============================================================================

run:
	@echo -------------------- $@ $$(date) --------------------
	uv run main.py

# ==============================================================================
# Code Quality
# ==============================================================================

lint:
	@echo -------------------- $@ $$(date) --------------------
	uv run ruff check .

format:
	@echo -------------------- $@ $$(date) --------------------
	uv run ruff format .

check: lint
	@echo -------------------- $@ $$(date) --------------------
	@echo All checks passed!

fix: format
	@echo -------------------- $@ $$(date) --------------------
	uv run ruff check --fix .
